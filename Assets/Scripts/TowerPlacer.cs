using System.Linq;
using Assets.Scripts.Entities;
using UnityEngine;

namespace Assets.Scripts
{
    public class TowerPlacer : MonoBehaviour
    {
        private void Update()
        {
//            var allHits = Physics.RaycastAll(Camera.main.ScreenPointToRay(Input.mousePosition));
//            var selected = allHits.FirstOrDefault(h => h.transform.GetComponent<Tower>() || h.transform.GetComponent<Creep>() || h.transform.GetComponent<Player>()).transform;
        }
    }
}